name: 🔁 Master Scraper Trigger

on:
  workflow_dispatch:  # manually triggered

jobs:
  trigger-all:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Trigger Batch 1
        uses: peter-evans/workflow-dispatch@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          workflow: scraper_batch1.yml
          ref: main

      - name: 🚀 Trigger Batch 2
        uses: peter-evans/workflow-dispatch@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          workflow: scraper_batch2.yml
          ref: main

      - name: 🚀 Trigger Batch 3
        uses: peter-evans/workflow-dispatch@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          workflow: scraper_batch3.yml
          ref: main

      - name: 🚀 Trigger Batch 4
        uses: peter-evans/workflow-dispatch@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          workflow: scraper_batch4.yml
          ref: main
